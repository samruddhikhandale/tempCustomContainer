{
    "build": {
		"dockerfile": "Dockerfile",
		"args": { "VARIANT": "focal" }
	},
    "containerUser": "mock-container",
	    "remoteUser": "mock-remote",
    "postCreateCommand": "echo `whoami`> whoami.txt",
}
